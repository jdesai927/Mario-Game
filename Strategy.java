
//Strategy followed by Ganondorf's AI. This had more members and was more complicated when Ganondorf's AI was different,
//but I've since modified it so that this enum isn't all that useful.

public enum Strategy {
	CHASEPLAYER, AVOIDPLAYER 
}
